{
  "name": "copilot-cross-reference",
  "displayName": "Copilot Cross-Reference",
  "description": "Open Microsoft Copilot inside VS Code and send prompts/selections for cross-reference.",
  "version": "0.0.1",
  "publisher": "local",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/smokydastona/micro_co-cross_vs-code_extension.git"
  },
  "homepage": "https://github.com/smokydastona/micro_co-cross_vs-code_extension",
  "bugs": {
    "url": "https://github.com/smokydastona/micro_co-cross_vs-code_extension/issues"
  },
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished",
    "onCommand:copilotCrossRef.open",
    "onCommand:copilotCrossRef.askSelection",
    "onCommand:copilotCrossRef.askPrompt",
    "onCommand:copilotCrossRef.setOpenAIApiKey",
    "onCommand:copilotCrossRef.clearOpenAIApiKey",
    "onCommand:copilotCrossRef.debateSelection",
    "onCommand:copilotCrossRef.debatePrompt"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "copilotCrossRef.open",
        "title": "Copilot Cross-Reference: Open Microsoft Copilot"
      },
      {
        "command": "copilotCrossRef.askSelection",
        "title": "Copilot Cross-Reference: Ask Microsoft Copilot About Selection"
      },
      {
        "command": "copilotCrossRef.askPrompt",
        "title": "Copilot Cross-Reference: Ask Microsoft Copilot (Prompt)"
      },
      {
        "command": "copilotCrossRef.setOpenAIApiKey",
        "title": "Copilot Cross-Reference: Set OpenAI API Key"
      },
      {
        "command": "copilotCrossRef.clearOpenAIApiKey",
        "title": "Copilot Cross-Reference: Clear OpenAI API Key"
      },
      {
        "command": "copilotCrossRef.debateSelection",
        "title": "Copilot Cross-Reference: Debate in ChatGPT (Selection)"
      },
      {
        "command": "copilotCrossRef.debatePrompt",
        "title": "Copilot Cross-Reference: Debate in ChatGPT (Prompt)"
      }
    ],
    "configuration": {
      "title": "Copilot Cross-Reference",
      "properties": {
        "copilotCrossRef.target": {
          "type": "string",
          "enum": ["web", "windows", "chatgpt"],
          "default": "web",
          "description": "Where to send prompts: 'web' opens copilot.microsoft.com; 'windows' copies prompt to clipboard and shows instructions for Windows Copilot; 'chatgpt' calls the OpenAI API and shows results in VS Code."
        },
        "copilotCrossRef.openaiModel": {
          "type": "string",
          "default": "gpt-4.1",
          "description": "OpenAI model to use when target is 'chatgpt'."
        },
        "copilotCrossRef.openaiBaseUrl": {
          "type": "string",
          "default": "https://api.openai.com/v1",
          "description": "Base URL for OpenAI API (only needed for proxies/enterprise gateways)."
        },
        "copilotCrossRef.onboardingPrompt": {
          "type": "boolean",
          "default": true,
          "description": "When enabled, prompts to set an OpenAI API key on startup if target is 'chatgpt' and no key is stored."
        },
        "copilotCrossRef.debateMaxRounds": {
          "type": "number",
          "default": 6,
          "minimum": 1,
          "maximum": 20,
          "description": "Maximum number of Critic→Builder rounds for Debate mode. The debate stops early when the Action Items checklist has no unchecked items."
        },
        "copilotCrossRef.copilotUrl": {
          "type": "string",
          "default": "https://copilot.microsoft.com/",
          "description": "Base URL for Microsoft Copilot web experience."
        },
        "copilotCrossRef.openInSimpleBrowser": {
          "type": "boolean",
          "default": true,
          "description": "Open Copilot inside VS Code using the built-in Simple Browser (falls back to external browser if unavailable)."
        },
        "copilotCrossRef.prefillQueryInUrl": {
          "type": "boolean",
          "default": false,
          "description": "Attempt to prefill the Copilot prompt using a querystring parameter. This may stop working if the Copilot website changes."
        },
        "copilotCrossRef.promptTemplate": {
          "type": "string",
          "default": "Please cross-check the following content and point out any issues, missing context, or better alternatives.\n\n{{text}}",
          "description": "Template used when sending text to Copilot. Use {{text}} as a placeholder for the selection/prompt."
        },
        "copilotCrossRef.copyPromptToClipboard": {
          "type": "boolean",
          "default": true,
          "description": "Copy the generated Copilot prompt to your clipboard (recommended, since prefill via URL is not guaranteed)."
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p .",
    "watch": "tsc -watch -p .",
    "package": "npm run compile && vsce package",
    "vscode:prepublish": "npm run compile"
  },
  "dependencies": {
    "openai": "^4.79.0"
  },
  "devDependencies": {
    "@vscode/vsce": "^3.6.0",
    "@types/node": "^20.11.30",
    "@types/vscode": "^1.90.0",
    "typescript": "^5.5.4"
  }
}
